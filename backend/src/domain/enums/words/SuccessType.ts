/**
 * Enum representing success types related to word operations.
 *
 * @enum
 */
export enum WordSuccessType {
  /**
   * Success type indicating that the word was deleted successfully.
   */
  WordDeleted = 'Word deleted with success!',
}
