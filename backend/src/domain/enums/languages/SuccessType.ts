/**
 * Enum representing success types related to language operations.
 *
 * @enum
 */
export enum LanguageSuccessType {
  /**
   * Success type indicating that the language was deleted successfully.
   */
  LanguageDeleted = 'Language deleted with success!',
}
