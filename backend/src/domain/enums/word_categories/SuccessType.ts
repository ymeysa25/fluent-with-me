/**
 * Enum representing success types related to word_category operations.
 *
 * @enum
 */
export enum WordCategorySuccessType {
  /**
   * Success type indicating that the word_category was deleted successfully.
   */
  WordCategoryDeleted = 'WordCategory deleted with success!',
}
